# Git Lab CLI-2

A simple analytics command-line application used for Git learning exercises.

The application loads a small metrics dataset and prints a report.

This repository intentionally evolves through multiple feature branches and releases to demonstrate advanced Git workflows.